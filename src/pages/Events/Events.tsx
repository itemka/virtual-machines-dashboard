import { Box, Typography } from '@mui/material';
import styles from './stytles.ts';

export function Events() {
  return (
    <Box sx={styles.pageContainer}>
      <Typography component="span">Events page is empty</Typography>
    </Box>
  );
}
