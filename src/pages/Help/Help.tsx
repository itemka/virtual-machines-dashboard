import { Box, Typography } from '@mui/material';
import styles from './stytles.ts';

export function Help() {
  return (
    <Box sx={styles.pageContainer}>
      <Typography component="span">Help page is empty</Typography>
    </Box>
  );
}
